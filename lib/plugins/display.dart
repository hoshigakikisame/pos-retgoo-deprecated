import 'package:flutter/services.dart';
import 'package:shared_preferences/shared_preferences.dart';

class CustomerDisplay {
  //static final platform =
      //MethodChannel("id.retgoo.flutter/display", JSONMethodCodec());

  static print(String text) async {
    //final sp = await SharedPreferences.getInstance();
    //String displayPort = sp.getString("display_port");
    //int baudRate = sp.getInt("display_baud_rate");
    //if (displayPort.isNotEmpty ?? true && baudRate != null) {
    //  await platform.invokeMethod("print", {
    //    "port": displayPort,
    //    "text": text,
    //    "baud_rate": baudRate,
    //  });
    //}
  }
}
